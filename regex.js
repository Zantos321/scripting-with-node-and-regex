module.exports = {
   getComponents(file) {
      return "use the string .match() method and regex to find what you are looking for"
   },
   getName(component) {
      return "use the string .match() method and regex to find what you are looking for"
   },
   getDesc(component) {
      return "use the string .match() method and regex to find what you are looking for"
   },
   getInputs(component) {
      return "use the string .match() method and regex to find what you are looking for"
   },
   trim(str) {
      return "use the string .match() method and regex to find what you are looking for"
   },
};
